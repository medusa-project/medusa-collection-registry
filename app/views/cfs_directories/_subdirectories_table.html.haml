- if @directories_helper.full_count > 0
  %table.table.table-striped#directory_subdirectories
    %thead
      %tr
        - @directories_helper.headers.each do |header|
          %th= header
  = render partial: 'shared_js/search_datatable_init', locals: {selector: '#directory_subdirectories', search_helper: @directories_helper}
- else
  %p No Folders