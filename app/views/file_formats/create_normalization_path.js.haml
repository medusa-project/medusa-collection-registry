- if @created
  $("#file_format_normalization_paths .panel-body").append("#{escape_javascript(render(partial: 'file_format_normalization_path', locals: {path: @normalization_path}))}")
- else
  alert("Error creating Normalization Path!");
