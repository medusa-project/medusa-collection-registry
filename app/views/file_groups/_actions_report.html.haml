- if can?(:export, @file_group)
  .btn-group
    = render 'shared/dropdown_toggle', title: :Report, icon: :table
    %ul.dropdown-menu.dropdown-menu-right{role: :menu}
      %li= link_to 'Manifest', report_manifest_cfs_directory_path(@directory), remote: true
      %li= link_to 'Map', report_map_cfs_directory_path(@directory), remote: true
