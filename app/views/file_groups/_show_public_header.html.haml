%h1 File Group
%p.lead Collection: #{link_to @collection.title, public_collection_path(@collection)}
- if @file_group.cfs_directory
  %p
    = link_to 'See files', public_cfs_directory_path(@file_group.cfs_directory)
