= render layout: 'layouts/page_header' do
  Package Profiles

= render layout: 'layouts/standard_table', locals: {id: :package_profiles, headers: ['Name', 'Url', '']} do
  - @package_profiles.each do |package_profile|
    - cache(package_profile) do
      %tr
        %td= link_to(package_profile.name, collections_package_profile_path(package_profile))
        %td= link_to(package_profile.url, package_profile.url)
        %td
          .btn-group
            = small_view_button(package_profile)
            = small_edit_button(package_profile)

%br

= link_to 'New Package Profile', new_package_profile_path, class: 'btn btn-primary'