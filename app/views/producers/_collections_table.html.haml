%h2
  Collections

%div
  = render 'layouts/standard_table', id: :collections, headers: ['External ID', 'Title', 'Repository', 'Contact', 'Size (GB)', ''] do
    - @producer.collections.each do |collection|
      %tr
        %td #{collection.external_id}
        %td #{link_to collection.title, collection}
        %td #{link_to collection.repository.title, collection.repository}
        %td #{collection.contact_email}
        %td #{collection.total_size.to_s(:rounded, precision: 3)}
        %td
          = small_edit_button(collection)
