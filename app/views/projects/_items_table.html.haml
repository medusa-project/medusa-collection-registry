%table.table.table-striped#items
  %thead
    %tr
      %th Barcode
      %th Bib Id
      %th Book Name
      %th Title
      %th Author
  %tbody
    - @items.each do |item|
      - cache(item) do
        %tr
          %td= link_to(item.barcode, item_path(item))
          %td= item.bib_id
          %td= item.book_name
          %td= item.title
          %td= item.author