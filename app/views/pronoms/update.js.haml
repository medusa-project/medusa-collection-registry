- if @updated
  $("#pronom_#{@pronom.id}").replaceWith("#{escape_javascript(render('table_row', pronom: @pronom))}");
  $("#file_format_metadata").replaceWith("#{escape_javascript(render('file_formats/metadata', file_format: @pronom.file_format))}");
- else
  alert("Error updating Pronom!");
