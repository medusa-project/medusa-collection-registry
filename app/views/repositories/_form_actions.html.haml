-# must pass f, the form
= submit_button(f)
= cancel_and_go_to_button(repositories_path)
-  if @repository.persisted? and safe_can?(:destroy, @repository)
  = delete_button(repository_path(@repository), message: t('repositories.confirm_message'))
