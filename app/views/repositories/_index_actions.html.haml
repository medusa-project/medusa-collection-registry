.btn-group.pull-right
  - if can? :update_ldap_admins, Repository
    = primary_button 'Edit Repository Admins', edit_ldap_admins_repositories_path
  .btn-group
    = render 'shared/dropdown_toggle', title: :Export, text: :Export
    %ul.dropdown-menu
      %li= link_to 'CSV', repositories_path(format: 'csv')
