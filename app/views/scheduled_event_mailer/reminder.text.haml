:plain

The Medusa system would like to remind you that the event:

#{@event.message}

is scheduled for the #{@eventable.decorated_class_human} #{@eventable.label}

on #{@event.action_date.to_s}.

You can view the event at:

#{url_for(only_path: false, action: 'events', controller: @eventable.decorated_class_plural_underscore, id: @eventable.id)}