:plain

The Medusa system would like to remind you that the event:

#{@event.message}

is scheduled for the #{@event.scheduled_eventable.class.to_s.underscore.humanize} #{@event.scheduled_eventable.label}

on #{@event.action_date.to_s}