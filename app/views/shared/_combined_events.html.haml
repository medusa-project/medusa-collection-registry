.row
  %table.table.table-striped#cascaded_events
    %thead
      %tr
        - @helper.headers.each do |header|
          %th= header
= render partial: 'shared/search_datatable_init', locals: {selector: '#cascaded_events', search_helper: @helper}

