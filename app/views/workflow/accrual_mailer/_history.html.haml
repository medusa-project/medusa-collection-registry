%h4 History

- if @workflow_accrual.workflow_accrual_comments.present?
  - @workflow_accrual.workflow_accrual_comments.each do |comment|
    %h5 #{comment.user.net_id} - #{comment.created_at}
    %p #{comment.body}
- else
  %p No comments